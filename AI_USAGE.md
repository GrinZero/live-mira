# AI 使用说明

本项目的主体实现由 Devin / SWE-2 完成，后续体验、回归和世界存档迭代又经过多次 Devin 与 Codex session。
人类提供了 PLAN.md 方向、内容层文案基调、关键体验反馈、API 凭证和验收标准，并对“是否自然、是否像真实相遇”做最终判断。

历史中有两条需要同时保留的线：Devin session 负责首版架构、Duplex 探针、核心实现和早期部署尝试；后续 session 针对真实浏览器表现继续修正了节奏、悬念泄露、音频、头部跟随、连续动作、转场和持久世界。不能只看 Git commit 复原完整编辑历史。

## 时间证据口径

- 首个 live-demo Devin CLI 工程 session 的日志起点是 `2026-09-16 00:26:45`（Asia/Shanghai，原始 UTC：`2026-09-15T16:26:45Z`）。
- 首次 Git 提交 `ecb9be0` 的时间是 `2026-09-18 14:50:32`（Asia/Shanghai）。两者相差 `62 小时 23 分 47 秒`，这是日历跨度，不是连续编码时长。
- `PLAN.md` 的 `72 小时`是首版实验预算；它不能替代人类实际投入工时。实际有效工时需要人工根据工作记录填写，不能从 session 启停时间直接推算。
- 首版提交后，Devin 在 `2026-09-18` 至 `2026-09-19` 仍有部署、复核、浏览器路线和世界存档相关 session；展示录屏文件的落盘时间为 `2026-09-18 16:53:15`，真实路线结果文件的落盘时间为 `2026-09-19 21:55:34`。这些是文件时间证据，不等同于人工工作时长。

## AI 参与了什么

- **架构落地**：浏览器 React/R3F 呈现层 ⇄ Node 服务端 ⇄ 豆包 Duplex 的三方编排；
  字幕、舞台/动作、媒体事件和音频播放的状态协调，epoch 打断守卫，以及服务端↔客户端双 VAD 打断路径。
- **协议探针**：`explore/probe_duplex*.py` 是 AI 写的试错脚本——先用小探针把
  Duplex 的握手、`speech_text_buffer.commit`、工具调用回传、`input_audio_mute.commit`
  保活、`session.close` 全部实测清楚，再开始写正式代码。
- **内容工程**：`content/` 的人设卡、导演提示词、事件池、台词和风格模板由 AI 起草；
  后续根据人工反馈从“固定事件池/快节奏剧情”转向开放式相遇，并增加节奏纪律和悬念揭示边界。
- **资产管线**：VRM 选型与姿态调校、Seedream 风格模板、ffmpeg 程序化环境音合成、
  MediaPipe FaceLandmarker（`@mediapipe/tasks-vision` + `face_landmarker.task`，
  Google 官方模型，摄像头人脸追踪全部在本地推理，画面不上传）驱动的"对视"视线追踪。
- **观测与回放**：服务端环形日志、录制器（`RECORD=1`）、回放包构建、MockTransport，
  以及真实服务/真实浏览器证据写入 `output/checks/` 和 `output/playwright/`，支持 Otel 全链路观测。

## 人工验证与决策点

- 人工反馈纠正了“很快问用户是否认识她”“安静时弹出一张卡片”“场景切换像平移/黑屏”“对话像预制问答”“雷声或嘟声异常”等体验问题；对应实现分别转为悬念门控、当前底图叠层、固定站位/遮挡转场、可取消的续话与反应、独立雷声资源及迟到音频保护。
- `output/checks/live-conversation.json`、`live-voice.json`、`live-world.json`、`live-media.json` 保留了真实服务检查；`output/playwright/walk/live/result.json` 保留了真实语音/导演/生图/地图往返的一次成功过程。
- `scripts/verify-world-browser.ts` 是隔离存档、fixture 语音的真实浏览器回归；它不能证明真实麦克风、物理手机帧率、热量或嘈杂环境中的回声消除。
- README 提供现有 Railway 部署入口；本轮未重新验收公网完整语音链路，不把域名存在等同于全流程验收。
- 2026-09-20：用户提供 `demo-live.mp4` 作为 4:07.550 主片，原样归档；Codex 独立补录 2:50.720 的回应 API 超时、生图 503、图片 404、语音服务断连及降级/恢复过程，使用明确标注的边界故障注入和真实健康服务。用户明确要求两个视频分开，未拼接。自动化操作与检查不冒充人工验收，具体证据和未确认项见 `docs/demo-delivery.md`。

## AI 犯的错与人工/实测纠正

> 以下每一条都来自真实测试暴露的问题，不是臆测：

1. **VRM 资产 URL 臆造**：AI 最初猜的 GitHub 路径返回 404（还差点把错误页当模型用）。
   纠正：改用仓库树和文件体积校验，最终由 `scripts/fetch-assets.mjs` 固定到
   `madjin/vrm-samples` 的 `AvatarSample_A.vrm`，并在 README 标明来源与可替换边界。
2. **生图尺寸被 API 拒绝**：`2016x1152` 低于 Seedream 4.5 最小像素数
   （3,686,400）。纠正：实测后改 `2560x1440`，并把尺寸提取为环境变量。
3. **导演输出不可解析**：LLM 偶尔返回非法 JSON、枚举外中文、括号舞台词。
   纠正：`parseDecision` 花括号配对提取 + `parseDirective` 枚举白名单 +
   中文关键词归一表 + `stripStage` 剥离括号文本（含 "a|b" 管道符容错）。
4. **打断"没生效"（最大的一个坑）**：首轮打断测试失败——排查发现 Duplex
   下行音频是**突发式**下发（3.5s 语音 450ms 内发完），`output_audio.done`
   到达时她其实还在"说"。如果按 done 就翻回倾听状态，打断窗口会被提前关闭。
   纠正：客户端按播放缓冲耗尽时刻切换状态；服务端在 done 后保留 6s 打断
   宽限窗。教训：流式协议的"事件完成"≠"用户感知完成"。
5. **npm 依赖地狱**：R3F/Drei 与 React 19.3 的 peer 约束冲突。纠正：锁定
   `react@~19.2` 兼容区间。
6. **ffmpeg 编码器名臆造**：`libvorbis` 在本机 ffmpeg 不存在。纠正：枚举
   `-encoders` 后回退 `libopus`/`vorbis`/`aac`。
7. **剧情节拍不稳定**：事件注入全靠导演 LLM 抽签时，"掏照片"这个关键演示
   节拍经常不发生。纠正：给 `or_user_asks` 加确定性触发通道（关键词命中即
   注入），并加 `photoArmed` 兜底——演员没自发调 `show_photo` 时服务端代触发。
8. **Playwright 参数解析**：`has()` 对 `--flag` 前缀处理错误导致截图参数
   被吞。纠正：统一 `argv.includes('--k')` 与 `arg()` 取值约定。
9. **把静态转场检查误当真实转场**：早期 harness 没有真正离开咖啡馆，截图虽然无报错，
   但背景始终没变、`scene.presented` 也没有发生。纠正：把该次检查标为失败，之后补跑真实
   服务和浏览器路径，并保留 `output/playwright/walk/live/result.json`；仍不把它扩大解释成实体手机验收。
10. **生成式场景不稳定**：早期生成结果可能带空边栏、人物或与当前地点不一致的构图。
    纠正：把环境描述与角色分开、使用当前底图做叠层编辑、固定输出尺寸并加像素/空白检测；
    失败时保留原地点或使用照片占位，而不是把坏图提交为已抵达场景。

11. **错误演示选题和角色台词不符合意图**：AI 最初补录了麦克风不可用，用户明确要求 API/服务故障。随后用户指出“刚才卡了一下”破坏角色逻辑，并指定“没听清”式自然圆场。纠正：改为“刚才你说什么？我没听清。”，移入内容台词库，增加失败后恢复与过期请求保护测试，重新录制四种服务/资源故障；旧片不作为交付。

## 人工仍需注意的边界

- 中文 ASR 对合成 TTS 测试音频偶有丢句；真实人声正常。
- `AvatarSample_A` 是 VRoid 官方示例模型，不是定制的拟人 Mira 皮肤；换装/换模只需替换
  `assets/mira.vrm` 并保持骨骼/表情命名兼容（VRM 标准）。
- `.env` 不入库（`.gitignore` 已加），回放包 `web/public/mock/` 可入库。
- `output/` 与 `recordings/` 默认被 `.gitignore` 排除；提交或评审时若需要演示视频/原始证据，
  需另外上传或按交付平台要求解除对应忽略，而不能假设 README 的相对路径在远端自动存在。
